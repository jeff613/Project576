how to use:

give argument of the folder where all the vdo.rgb files reside
click generate strips will generate all vdo.strip files to the folder
click color indexing will generate all vdo.color files to the folder
click motion indexing will generate all vdo.motion files to the folder
click sound indexing will generate all vdo.audio files to the folder

and then pass the folder name as the third parameter for the video_player(see readme in Video_Player)